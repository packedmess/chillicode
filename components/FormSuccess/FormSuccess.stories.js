import FormSuccess from './FormSuccess';

export default {
  title: 'App/FormSuccess',
  component: FormSuccess,
  argTypes: {},
};

const Template = args => <FormSuccess {...args} />;

export const Default = Template.bind({});
Default.args = {};
